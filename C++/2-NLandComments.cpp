#include <iostream>
using namespace std;

int main() {
    cout << "the c++ languange doesn't auto break line, ";
    cout << "to do so, you have to use the \nor << endl" << endl;
    //you can also comment using the double slash(//), the comments will be ignored by the compiler
   
   return 0;
}